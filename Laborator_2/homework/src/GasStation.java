public class GasStation extends Location{
    /**
     * Creates a new GasStation object
     *
     * @param name A String representing the name of the GasStation
     * @param x    An int representing the x coordinate of the GasStation
     * @param y    An int representing the y coordinate of the GasStation
     */
    GasStation(String name, int x, int y) {
        super(name, "Gas station", x, y);
    }
}
